from lib.test import assert_eq

vals = {x for x in 1..9}
assert_eq(#vals, 9)
assert_eq(vals[1], 1)
assert_eq(vals[9], 9)

evens = {x for x in 1..9 if x % 2 == 0}
assert_eq(#evens, 4)
assert_eq(evens[1], 2)
assert_eq(evens[4], 8)

print "comprehension range ok"
