from lib.test import assert_eq

string = import string

t = { base = 2 }
t.add = fn(self, x)
  return self.base + x

t.mul = fn(x, y)
  return x * y

assert_eq(t.add(3), 5)
assert_eq(t.mul(2, 3), 6)

assert_eq("abc".upper(), "ABC")
