# `math` Module

Import:

```pua
math = import math
```

## Constants

- `math.pi`
- `math.huge`

## Functions

- `sin(x)`
- `cos(x)`
- `tan(x)`
- `asin(x)`
- `acos(x)`
- `atan(y)`
- `atan(y, x)`
- `sqrt(x)`
- `floor(x)`
- `ceil(x)`
- `abs(x)`
- `exp(x)`
- `log(x)`
- `log(x, base)`
- `pow(x, y)`
- `fmod(x, y)`
- `divmod(x, y) -> {q, r}`
- `modf(x) -> int_part, frac_part`
- `deg(rad)`
- `rad(deg)`
- `random()`
- `random(max)`
- `random(min, max)`
- `seed([number])`
- `min(...)`
- `max(...)`
- `sum(...)`
- `sum(table)`
