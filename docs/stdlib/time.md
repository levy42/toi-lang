# `time` Module

Import:

```toi
time = import time
```

## Functions

- `time.time() -> seconds` (alias of `seconds`)
- `time.seconds() -> number`
- `time.micros() -> number`
- `time.nanos() -> number`
- `time.sleep(seconds) -> nil`
- `time.clock() -> number`
